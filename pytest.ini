[pytest]
minversion = 7.0
testpaths = tests
pythonpath = src
addopts = -q -m "not fake_live_slow"
asyncio_mode = auto
markers =
    fake_live: fast fake-live harness tests included in the default suite
    fake_live_slow: slow full fake-live harness tests excluded from the default suite
