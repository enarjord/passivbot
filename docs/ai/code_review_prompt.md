# Code Review Compatibility Route

This path is temporarily retained for external agents that still load the former checklist.

Use `validation.md` for the change-class validation matrix and review method. Autonomous reviewers
also read `runbooks/pr_review.md`. Task-specific contracts are routed by `README.md`.

Remove this compatibility route only after known external consumers have migrated.
